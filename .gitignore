.DS_Store
.idea/
.vscode/

node_modules/
.pnpm-store/
.playwright-cli/
output/
dist/
coverage/
target/
# electron-vite 的产物与 electron-builder 的打包输出（repo.rules.json 也拒绝跟踪）
out/
apps/desktop/release/
# scripts/stage-binaries.mjs 拷贝进来的四个 sidecar 二进制，供 electron-builder 的
# extraResources 打包；与 target/release 里的产物同源，不入库
apps/desktop/resources/

.env
.env.*
!.env.example

*.log
*.db
*.db-shm
*.db-wal

# 工作空间里的应用私有目录（板日志、导出的画图 PNG、落盘的图片）
.armadra/

# Claude Code 本机私有设置（impeccable 安装器写入的 hook）
.claude/settings.local.json
target-main/
