# 桌面界面与交互规范

本轮以 Codex App 的桌面工具风格为方向：系统字体、中性灰表面、清晰的分组与低噪声图标。保留 Armadra 的画布与原生终端，不将它改成聊天产品。Impeccable 4.1.3 已安装于 `.claude/skills/impeccable`；使用其 polish、animate 和 craft-floor 规则审查已有界面，而不是引入另一套组件库。基础控件仍使用 shadcn / Radix。

## 五处问题与修复

| 问题                     | 原因                                                                   | 当前行为                                                                                            |
| ------------------------ | ---------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| 左上图标错位             | 原生红绿灯的 inset 与网页按钮不共享同一中心线，按钮尺寸也不同          | 原生 inset 校准到 44px 标题栏；网页图标统一 28px                                                    |
| 右侧工具栏过低           | 写死 top:54px                                                          | 第一颗图标与左侧标题栏图标中心对齐                                                                  |
| 额度数据、时间和位置错误 | 忽略模型专属窗口、未知标签回退错误、失败时记录成功时间、缓存与刷新混淆 | Claude/Codex 的有效窗口分别显示已用额度与重置时间；展示真实采集时间、过期和失败状态；球在缩略图左侧 |
| 小窗口缩略图消失         | 旧画布引擎的默认导航面板在窄断点不挂载缩略图                           | 自定义宿主保证始终可达；支持收为 36px 图标并记住状态                                                |
| 底部工具栏遮挡其他控件   | 相对整个窗口居中，未扣除侧栏与导航区域                                 | 改为实际画布容器布局；窄窗口绘图工具收进可键盘操作的面板，并与导航错开                              |

## 布局契约

- 左侧栏宽度 240px，标题栏 44px，Dock 高度 44px；布局以侧栏以外的 `.workspace-surface` 为容器。
- 画布宽度大于 1000px 时，Dock 使用扣除右侧导航后的空间。更窄时，导航放在 Dock 上方，绘图按钮收进工具面板；工具没有被删除。
- 缩略图正常为 200×150px；窄画布缩为 160×120px；用户收起时只占 36×36px。球体跟随缩略图边缘，而不是另写一个固定偏移。
- 节点按内容的标准尺寸新建（浏览器 1280×800、终端 960×600，全表见[计划书 §3.4](../contracts/v3-agent-terminal-plan.md)），落在视口中心；缩放低于 0.5 时相机对准新节点并抬到 100%。外壳只留必要的一条：头部 30px、折叠 32px、头部图标钮 24px、终端内边距 4px、浏览器工具栏 28px，其余高度归内容，终端字号默认 12px。
- 设置页使用导航和内容两栏。内容独立滚动；空间不足时控件可以换行，极窄窗口使用带可访问名称的图标导航。
- 浅色为白色画布与中性灰侧栏；深色为层次分明的灰色表面。ANSI 终端保留深色背景，避免 CLI 的颜色对比失效。

## 开屏动画

新进程打开窗口时先放一段 4 秒的开屏动画（`apps/web/src/splash/`）：手写签名写完后整体下移，犰狳从左侧遮罩后走出两步，停稳并淡成品牌标识。位图素材是独立资源文件，字形与时间轴在源码里。

- 时机：`main.tsx` 一执行就挂，不等 Runtime 握手；App 在覆盖层下方照常挂载与拉数据。同一页面会话只放一次（`sessionStorage` 的 `armadra.splash.shown`），所以 ⌘W / 托盘隐藏后恢复窗口不会重播，开发热重载与刷新也不会；⌘Q 之后重新打开是新 WebView，动画照放。桌面壳不为此新增 IPC。
- 主题：`armadra.theme` 为 `system` 时按 `prefers-color-scheme` 选深浅，播放期间跟随系统切换；用户明确选了浅色或深色就按用户的。配色对齐 `styles/tokens.css` 的 `--bg` / `--text` / `--brand` / `--brand-text`。
- 行为：全屏覆盖层，播完淡出并从 DOM 摘除；播满 1 秒后点击或按键可跳过；`prefers-reduced-motion` 下只显示终态约 1 秒。画面等比缩放居中，窄窗口不横向溢出。
- 开关：设置 → 通用的「启动动画」，默认开，存在本地偏好 `armadra.splashAnimation`；关掉后不再展示。

## 交互与数据

连线预览与持久化链接使用同一组边缘端口与 Bézier 几何；精确锚点在 binding 创建与修改前规范化，不让终端间连线先吸到中心再跳回边缘。白板自身的箭头是 `line` 对象上的 `arrowStart` / `arrowEnd`，与节点连线各走各的几何。

缩略图与球体位置使用短时过渡；减少动态效果偏好下关闭过渡。工具菜单与用量详情均可通过键盘进入；用量面板支持悬停预览、点击固定、Escape 关闭并恢复焦点。设置中的默认权限按每种 CLI 的实际能力筛选，不支持的模式不会静默执行。

Runtime 的额度刷新串行化并执行 30 秒冷却，后台每 5 分钟查询；前端周期性读取缓存，不把缓存读取时间当成额度更新时间。禁用查询不会伪装成账户登出。OpenCode、Pi、OMP 没有接入统一额度 API，设置页提供查看提示而不是假百分比。

## 验证方法

使用脚本启动源码和独立临时数据目录，避免误操作已安装的应用或用户正在工作的终端。布局检查覆盖 1440×920、1280×720、960×600 和更窄的浏览器窗口、深浅主题、侧栏及缩略图展开/收起；检查控件边界不重叠。桌面标题栏用当前 debug 二进制核验。

自动测试覆盖模型窗口映射、过期与刷新错误、键盘交互、连线端口与预览几何一致、真实 CLI 参数和协作协议边界。真实进程树测试需要允许执行系统 `ps`，沙箱拒绝不等于产品断言失败。

参考：[Impeccable](https://impeccable.style/)、[Codex App 设置](https://developers.openai.com/codex/app/settings/)。Armadra 的额度查询与节点协作实现、具体 CLI 能力与协议来源见 [协作设计](./agent-collaboration.md)。
